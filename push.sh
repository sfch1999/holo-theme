#!/bin/bash

# Navigate to the git repository (optional)
# cd /path/to/your/repository

# Add all changes
git add .

# Commit with a default message
git commit -m "Commit"

# Push to the current branch
git push
